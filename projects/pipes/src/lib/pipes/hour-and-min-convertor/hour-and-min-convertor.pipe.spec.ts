import { HourAndMinConvertorPipe } from './hour-and-min-convertor.pipe';

describe('HourAndMinConvertorPipe', () => {
  it('create an instance', () => {
    const pipe = new HourAndMinConvertorPipe();
    expect(pipe).toBeTruthy();
  });
});
